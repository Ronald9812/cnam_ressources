
int		function_name(int paramter0, char parameter1); //declaration


int		function_name(int paramter0, char parameter1)
{
	//the brackets hold the definition of the function
	int		var; //variable declaration

	var = 15; //definition - initialiazing variable0
	return (variable);
}

int		f_add(int a, int b)
{
	if (a == 0)
		return (b);
	if (!b)
		return (a);
	return (a + b);
}

int		f_divide(unsigned int a, int b)
{
	if (b == 1)
		return (a);
	if (b == 0)
		return (-1);
	return (a / b);
}
